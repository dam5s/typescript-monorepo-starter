{
    "name": "backend",
    "dependencies": {
        "@hapi/hapi": "^20.2.1"
    },
    "devDependencies": {
        "@types/hapi__hapi": "^20.0.10",
        "nodemon": "^2.0.15"
    },
    "scripts": {
        "start": "tsc --watch & nodemon dist/index.js",
        "lint": "eslint src --ext .ts,.tsx",
        "test": "jest",
        "build": "tsc"
    }
}
